spring:
  application:
    name: Email Service
  profiles:
    active: dev
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  liquibase:
    enabled: true
    changeLog: liquibase/master.xml
  mail:
    host: smtp.gmail.com
    port: 587
    username: ***************@gmail.com
    password: ****************
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  rabbitmq:
    addresses: amqps://kjrcondq:gVITtKja_4S9vAxaTypaGza2b1pjQ6dx@shrimp.rmq.cloudamqp.com/kjrcondq
    queue: email-service

server:
  port: 9091
  error:
    include-message: always

---

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/email-service
    username: postgres
    password: postgres